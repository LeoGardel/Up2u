<%= link_to "Sair", destroy_usuario_session_path, :method => :get %>
<!--
<h1> Areas </h1>

<section>
	<% @areas.each do |ei| %>
		<h3><%= ei.id %> - <%= ei["nome"] %></h3>
		<%= ei["descr"] %>
		<br>
	<% end %>
</section>
-->